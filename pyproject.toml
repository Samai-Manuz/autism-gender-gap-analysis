[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "autism-diagnosis-gender-gap"
version = "0.1.0"
description = "Data analysis project on autism diagnosis gender gap"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["src"]